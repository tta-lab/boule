version: "2"
sql:
  - engine: sqlite
    queries: "db/queries/"
    schema: "db/migrations/"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
        emit_empty_slices: true
        overrides:
          - db_type: "TEXT"
            go_type: "string"
